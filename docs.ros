#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn
  (ros:ensure-asdf)
  (asdf:load-asd (merge-pathnames "support-dapla-deploy.asd" *load-pathname*))
  #+quicklisp
  (ql:quickload '(:support-dapla-deploy/docs :40ants-doc :40ants-doc-full) :silent t))

(defpackage :ros.script.docs
  (:use :cl))

(in-package :ros.script.docs)

(defun main (&rest argv)
  (declare (ignore argv))
  (40ants-doc-full/gh-pages:update-asdf-system-docs
   :support-dapla-deploy/docs
   :root-section 'support-dapla-deploy/docs::@support-dapla-deploy
   :output-dir (merge-pathnames "docs/" (asdf:system-source-directory :support-dapla-deploy/docs))))
;;; vim: set ft=lisp lisp:
